<Window x:Class="MediaPlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MediaPlayer"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
        mc:Ignorable="d"
        Title="MainWindow"
        WindowState="Maximized"
        Height="768"
        Width="1024"
        MinHeight="600"
        MinWidth="800">

    <Grid Background="{StaticResource DarkBlue2}">
        <Grid.RowDefinitions>
            <RowDefinition Height="165*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="20*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="169*"/>
            <ColumnDefinition Width="471*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Background="{StaticResource DarkBlue}" Grid.Row="0">

            <Viewbox VerticalAlignment="Top" HorizontalAlignment="Left">

                <Label
                    Width="100"
                    Foreground="{StaticResource White}"
                    Content="Playlists"
                />
                
            </Viewbox>

            <Viewbox VerticalAlignment="Top" HorizontalAlignment="Left">

                <ListBox ItemContainerStyle="{StaticResource playlistsItem}" Style="{StaticResource playlists}" SelectedItem="{Binding CurrentPlaylistTitle, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}" ItemsSource="{Binding Playlists}" x:Name="Playlists" Width="200">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="SelectionChanged">
                            <i:InvokeCommandAction Command="{Binding SelectPlaylist}" CommandParameter="{Binding ElementName=Playlists, Path=SelectedItem}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </ListBox>

            </Viewbox>

        </StackPanel>

        <StackPanel Grid.Column="1">

            <Viewbox Margin="10" HorizontalAlignment="Left" MaxHeight="50">

                <Button Style="{StaticResource playButton}" Content="Play" Command="{Binding Play}" />

            </Viewbox>
            
            <Viewbox HorizontalAlignment="Left" VerticalAlignment="Top" MaxWidth="500">

                <ListBox Style="{StaticResource songs}" ItemContainerStyle="{StaticResource songsItem}" SelectedItem="{Binding CurrentSongName, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}" x:Name="SongsList" ItemsSource="{Binding Playlist}">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="SelectionChanged">
                            <i:InvokeCommandAction Command="{Binding SelectSong}" CommandParameter="{Binding ElementName=SongsList, Path=SelectedItem}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </ListBox>

            </Viewbox>

        </StackPanel> 

        <DockPanel Background="{StaticResource DarkBlue}" Grid.Row="2" Grid.ColumnSpan="2">

            <Viewbox>

                <DockPanel>

                    <Label Content="{Binding Position, Converter={StaticResource DateConverter}}" Style="{StaticResource label}" Foreground="{StaticResource LightBlue}" />

                    <Slider Style="{StaticResource positionSlider}" Maximum="{Binding Duration}" Value="{Binding Position}">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="PreviewMouseDown">
                                <i:InvokeCommandAction Command="{Binding Pause}"/>
                            </i:EventTrigger>
                            <i:EventTrigger EventName="PreviewMouseUp">
                                <i:InvokeCommandAction Command="{Binding Play}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </Slider>

                    <Label Content="{Binding Duration, Converter={StaticResource DateConverter}}" Style="{StaticResource label}" Foreground="{StaticResource Gray}" />

                </DockPanel>
                    
            </Viewbox>

        </DockPanel>

        <!--<StackPanel VerticalAlignment="Center">

            <DockPanel HorizontalAlignment="Center">

                <Button
                    Command="{Binding Play}"
                    Width="100"
                    Height="50"
                    Content="play"
                    FontSize="25"
                />

                <Button
                    Command="{Binding Stop}"
                    Width="100"
                    Height="50"
                    Content="stop"
                    FontSize="25"
                />

                <Button
                    Command="{Binding Pause}"
                    Width="100"
                    Height="50"
                    Content="pause"
                    FontSize="25"
                />

                <CheckBox IsChecked="{Binding IsPlayNextSong}" />

            </DockPanel>

            <StackPanel Height="140">
                <ListBox SelectedItem="{Binding CurrentSongName, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}" x:Name="SongsList" Height="70" Background="Bisque" ItemsSource="{Binding Playlist}">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="SelectionChanged">
                            <i:InvokeCommandAction Command="{Binding SelectSong}" CommandParameter="{Binding ElementName=SongsList, Path=SelectedItem}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </ListBox>

                <ListBox SelectedItem="{Binding CurrentPlaylistTitle, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}" ItemsSource="{Binding Playlists}" Height="70" Background="DarkGoldenrod" x:Name="Playlists">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="SelectionChanged">
                            <i:InvokeCommandAction Command="{Binding SelectPlaylist}" CommandParameter="{Binding ElementName=Playlists, Path=SelectedItem}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </ListBox>
            </StackPanel>

            <Slider Minimum="0" Maximum="1.0"  Width="100" Margin="20" Value="{Binding Volume, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />

            <Slider Margin="50" Minimum="0" Maximum="{Binding Duration}" Width="300" Value="{Binding Position, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="PreviewMouseDown">
                        <i:InvokeCommandAction Command="{Binding Pause}"/>
                    </i:EventTrigger>
                    <i:EventTrigger EventName="PreviewMouseUp">
                        <i:InvokeCommandAction Command="{Binding Play}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Slider>

            <TextBox  Text="{Binding NewTitle}"/>

            <Label Content="{Binding Position, UpdateSourceTrigger=PropertyChanged}" />
            
            <Label Content="{Binding CurrentSongName, UpdateSourceTrigger=PropertyChanged}" />

            <DockPanel HorizontalAlignment="Center">

                <Button x:Name="bt1" Height="30" Width="30" Margin="10" Content="back" Command="{Binding BackSong}" />

                <Button Height="30" Width="30" Margin="10" Content="next" Command="{Binding NextSong}" />
                
                <Button Height="30" Width="70" Margin="10" Content="AddPlaylist" Command="{Binding AddPlaylist}" />
                
                <Button Height="30" Width="60" Margin="10" Content="AddMusic" Command="{Binding AddMusic}" />

                <Button Height="30" Width="80" Margin="10" Content="RemoveMusic" Command="{Binding RemoveMusic}" />

                <Button Height="30" Width="90" Margin="10" Content="RemovePlaylist" Command="{Binding RemovePlaylist}" />

                <Button Height="30" Width="90" Margin="10" Content="Rename" Command="{Binding RenamePlaylist}" />

            </DockPanel>


        </StackPanel>-->

    </Grid>
</Window>
